# DEV — chargé automatiquement par `npm run dev` (mode development)
# Identifiants de démonstration locaux uniquement (ne pas réutiliser en production).

VITE_APP_ENV=development
VITE_APP_USERNAME=admin
VITE_APP_PASSWORD=demo
