# STAGING — copier vers .env.staging pour build local, ou configurer sur Netlify (contexte staging).
# Utiliser des identifiants distincts de la production.

VITE_APP_ENV=staging
VITE_APP_USERNAME=
VITE_APP_PASSWORD=

# Optionnel
# VITE_SENTRY_DSN=
# VITE_APP_DATA_URL=/data/agents.json
